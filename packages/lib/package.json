{
  "name": "@somos/lib",
  "version": "0.0.0",
  "private": true,
  "description": "Shared business logic (booking/subscription/pricing calculations, role/permission helpers). Empty scaffold in Phase 0 — logic lands in Phase 2+ (05-MODULE-BOOKING.md, 08-MODULE-FINANCE.md).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint . --ext .ts --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo '@somos/lib has no tests yet (Phase 0)'",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@somos/types": "workspace:*"
  },
  "devDependencies": {
    "@somos/config": "workspace:*",
    "eslint": "8.57.0",
    "typescript": "5.5.4"
  }
}
