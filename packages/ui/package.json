{
  "name": "@somos/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Shared design-system implementation: tokens + minimal base components (04-DESIGN-SYSTEM.md).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./src/tokens.css"
  },
  "scripts": {
    "lint": "eslint . --ext .ts,.tsx --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo '@somos/ui has no tests yet (Phase 0)'",
    "clean": "rm -rf dist .turbo"
  },
  "peerDependencies": {
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@somos/config": "workspace:*",
    "@types/react": "18.3.5",
    "eslint": "8.57.0",
    "react": "18.3.1",
    "typescript": "5.5.4"
  }
}
